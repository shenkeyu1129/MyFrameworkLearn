# QFramework 高效学习路线

## 总策略

不再按 API 或零散知识点学习，而是围绕一个持续增长的小型 Unity 项目学习：

> 资源收集小游戏：移动、拾取资源、显示数量、背包、任务、场景切换、保存数据。

先用最直接的 Unity/C# 写出功能，再在真实痛点出现时重构，最后把每次重构映射到 QFramework。这样学习的是“判断什么时候需要抽象”，不是“记住抽象的名字”。

## 每个迭代的固定流程

1. **需求卡**：只写一个用户可感知的功能和验收条件。
2. **直写版本**：允许代码简单、集中，先让功能跑起来。
3. **故障实验**：主动增加一个变化，例如第二个 UI、第二个场景、重复监听或动态生成对象。
4. **定位痛点**：记录耦合、重复、时序、测试困难或资源管理问题。
5. **最小重构**：只引入解决当前痛点所需的一个概念。
6. **对照实现**：用 QFramework 的对应能力替换这一小块。
7. **复盘卡**：写下收益、代价、适用边界和下一步问题。

## 阶段与产出

### 阶段 0：工具与最小闭环（半天）

产出：场景中一个可运行的资源收集原型。

只掌握完成闭环所需的内容：脚本挂载、序列化字段、输入、碰撞/触发、日志、基本调试。生命周期只在实际用到时解释，不单独背诵。

### 阶段 1：直写单体版本（1-2 个迭代）

产出：一个脚本可以让玩家移动、拾取资源、更新分数和 UI。

目标不是写得漂亮，而是建立“需求 -> 状态 -> 行为 -> 表现”的完整因果链，并学会通过日志和断点定位问题。

### 阶段 2：第一次拆分（2-3 个迭代）

产出：把规则、输入、表现拆开；规则部分可以脱离 Unity 测试。

只引入普通 C# 类、接口或事件中真正需要的部分。每次拆分都必须回答：原来的哪一行代码造成了什么问题？新边界解决了哪个具体问题？

### 阶段 3：MVC 与手写小架构（3-4 个迭代）

产出：背包和任务功能，包含 Model、View、Controller 的可运行版本。

自己实现一个很小的模块注册、事件通知、命令执行和生命周期入口。目的不是造轮子，而是让你能解释框架中的每个零件为什么出现。

### 阶段 4：逐件映射 QFramework（4-6 个迭代）

按痛点逐个替换，不一次性导入整个框架：

1. 用 Architecture 管理模块边界和依赖。
2. 用 Model 保存状态，用 System 放业务流程，用 Utility 放无状态能力。
3. 用 Command 表达一次业务动作，用 Event 传递状态变化。
4. 用 UIKit 管理界面生命周期和打开/关闭。
5. 用 ResKit 管理资源加载和释放。
6. 用 ActionKit 组织时间序列和可组合动作。

每次替换只保留一个可运行功能，并与手写版本对比调用链、可测试性、隐式行为和调试成本。

### 阶段 5：源码阅读与扩展（持续）

每次只追踪一个真实调用：从业务代码入口开始，向下跟踪注册、查找、执行、生命周期和错误边界。最终独立完成一个小扩展，并写出使用约束和失败案例。

## 单次学习节奏

每次 45-90 分钟：

- 10 分钟：阅读需求和验收条件
- 25-45 分钟：自己实现并运行
- 10 分钟：故障实验或变更需求
- 10-20 分钟：代码复盘与重构

卡住时采用提示阶梯：

1. 只指出要观察的现象
2. 给关键词、API 名称或排查路径
3. 给局部伪代码
4. 只有明确要求时才给完整参考实现

## 每个迭代的完成标准

必须能回答：

- 功能的最小验收条件是什么？
- 哪个对象拥有这份状态？
- 谁可以修改它？
- 变化如何通知表现层？
- 如果增加一个需求，哪段代码最先变脆？
- 当前抽象带来了什么成本？
- QFramework 替换后到底少了什么重复工作，又增加了什么隐式行为？

## 当前重启点

前面的 `Provider/Consumer` 和 `CounterModel` 实验保留为“实验样本”，不再作为主线。下一次从阶段 0 开始：先做资源收集小游戏的最小闭环，然后每次只推进一个可见功能。
